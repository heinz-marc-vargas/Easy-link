OC.clearForms();
$(".main_content").empty().append('<%= escape_javascript(render(:partial => "/products/supplier"))%>');

oTableUsers = $('#dt_e').dataTable( {
  "bProcessing": true,
  "bServerSide": true,
  "sPaginationType": "bootstrap",
  "sAjaxSource": "/products/unassociated.json",
  "fnDrawCallback": function( oSettings ) {
      $('.pop_over').popover();
    }
} );

$(function() {
  $('.pop_over').popover('destroy');
});
