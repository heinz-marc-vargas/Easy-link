<% unless @error.empty?%>
  html = '<div class="alert alert-error">';
  html += "<a class='close' data-dismiss='alert'>×</a>";
  html += "<%= @error %>" + '</div>';
  $("#product-errors").empty();
  $("#product-errors").append(html);
<% else %>
  OC.clearForms();
  $.sticky("<%= t('isc_product')%> <%= t(:successfully_created) %>.", {autoclose : 5000, position: "top-right", type: "st-success" });
  OC.getCartProducts();
<% end%>
